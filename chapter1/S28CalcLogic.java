package calcapp.logics; // クラスをパッケージに所属させる

public class S28CalcLogic {
    // S28CalcMainで呼び出すモジュールを定義
    public static int tasu(int a, int b) {
        return (a + b);
    }
    public static int hiku(int a, int b) {
        return (a - b);
    }
}
